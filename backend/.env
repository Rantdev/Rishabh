# MongoDB Atlas connection — replace placeholders before running
# DO NOT commit this file to source control if it contains real credentials.

# Connection string using the credentials you provided (keep this file local and private)
MONGO_URL="mongodb+srv://guptarantdev_db_user:io8AOLc8zbyGqMDr@cluster0.jmk9gng.mongodb.net/chai_maska_db?retryWrites=true&w=majority"
DB_NAME=chai_maska_db

# CORS origins used by the FastAPI app (comma-separated). Use specific origins in production.
CORS_ORIGINS=*
